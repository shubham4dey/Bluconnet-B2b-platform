# Production API URL baked into Vercel builds (Vite auto-loads .env.production).
# Override via the VITE_API_URL environment variable in the Vercel dashboard if needed.
VITE_API_URL=https://bluconnet-b2b-platform.onrender.com/api